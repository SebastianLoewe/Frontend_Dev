var x;
var y;

function setup() {
  createCanvas(600, 800);
  background(220);

}

function draw() {

  //–––––– nested loop

  for (x = 0; x <= width; x += 30) {
    for (y = 0; y <= height; y += 30) {

      circle(x, y, 25);

    }

  }
}
